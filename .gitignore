node_modules/
dist/
# Environment deploy bundles derived from the tested dist/
dist-staging/
dist-production/
.env
.env.*
!.env.example
*.log
.DS_Store
# Generated production fingerprint
frontend/public/build-info.json

# Browser-test output
playwright-report/
test-results/
ci-diagnostics-*/

# Never commit server-side secrets/private runtime data
**/osameh-portfolio-secrets.php
private/

# Lighthouse output. CI writes its report into the workspace so a failing run
# can upload it as an artifact; local runs produce the same throwaway files.
*-lighthouse.json
lighthouse-report.*
