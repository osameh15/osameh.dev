node_modules/
dist/
# Environment deploy bundles derived from the tested dist/
dist-staging/
dist-production/
.env
.env.*
!.env.example
*.log
.DS_Store
# Generated production fingerprint
public/build-info.json

# Never commit server-side secrets/private runtime data
**/osameh-portfolio-secrets.php
private/

# Local Lighthouse runs. CI writes its own report to /tmp and reads the scores
# from there, so these are throwaway local output.
*-lighthouse.json
